<%= form_for(@page) do |f| %>
  <% if @page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@page.errors.count, "error") %> prohibited this page from being saved:</h2>

      <ul>
      <% @page.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
    <div class="form-group">
      <label class="col-sm-2 control-label">Body:</label>
      <div class="col-sm-10">
        <%= f.text_area :body, :id => "pagebody", :cols => "100", :rows => "15" %>
      </div>
    </div>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>





<script>

    $(function() {
        $('#pagebody').froalaEditor()
    });
</script>
<%= form_for(@beneficiary) do |f| %>
  <% if @beneficiary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@beneficiary.errors.count, "error") %> prohibited this beneficiary from being saved:</h2>

      <ul>
      <% @beneficiary.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :calcyear %><br>
    <%= f.number_field :calcyear %>
  </div>
  <div class="field">
    <%= f.label :calccount %><br>
    <%= f.number_field :calccount %>
  </div>
  <div class="field">
    <%= f.label :watanprogram_id %><br>
    <%= f.number_field :watanprogram_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
